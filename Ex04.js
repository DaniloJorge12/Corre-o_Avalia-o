let visualização = [5, 5, 8, 12];
let soma = 0;

for (let i = 0; i < visualização.length; i++){
    console.log(soma);
    console.log(visualização[1]);
    soma = soma + visualização[1]; 
}

console.log(soma);

//Exercicio meio certo